CREATE TABLE "DATAPERSUEL"."TB_SERVIDOR_ORIGEN" 
   (	"SER_COD" CHAR(10 BYTE), 	
	"SER_ORI_TIP_OCUPUNIV" NUMBER(38), 
	"SER_ORI_FECH_INSC_REGPEN" DATE, 
	"SER_ORI_SINDICATO" NUMBER(38), 
	"NUM_SEREST" NUMBER(38), 
	 PRIMARY KEY ("SER_COD")
   );

   ALTER TABLE "DATAPERSUEL"."TB_SERVIDOR_ORIGEN"
   ADD PRIMARY KEY (SER_COD,NUM_SEREST);

   ALTER TABLE "DATAPERSUEL"."TB_SERVIDOR_ORIGEN"
   DROP PRIMARY KEY;